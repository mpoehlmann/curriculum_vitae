\ProvidesPackage{preamble}

% Setup document and layout
\usepackage[T1]{fontenc}
% \usepackage[tl, step=1cm]{pagegrid}
\usepackage[letterpaper, top=0.75in, bottom=0.75in, left=0.75in, right=0.75in]{geometry}  % showframe

% Colors
\usepackage{xcolor}
\definecolor{linkcolour}{rgb}{0,0.2,0.6}

% Other packages
% \usepackage{array}
\usepackage[style=phys, biblabel=brackets, eprint=true, articletitle=false, doi=false, url=false, maxnames=3]{biblatex}
\usepackage{colortbl}
\usepackage{enumitem}
\usepackage{fancyhdr}
\usepackage{fontspec}
\usepackage[colorlinks=true, breaklinks=true,linkcolor=linkcolour, citecolor=linkcolour, urlcolor=linkcolour]{hyperref}
\usepackage{lastpage}
\usepackage{parskip}  % Remove indentation from paragraphs
\usepackage{soul}
\usepackage{tabularx}
\usepackage{titlesec}
\usepackage{xspace}

% Fonts (fontspec package)
% \renewcommand{\familydefault}{\sfdefault}
% \setsansfont{Proxima Nova}
% \setsansfont{Angie Sans}
\setmainfont[
  BoldFont={Fontin Bold},
  ItalicFont={Fontin Italic},
  SmallCapsFont={Fontin SmallCaps},
]{Fontin Regular}

% Bibliography (biblatex package)
\addbibresource{pubs.bib}

% Sections (titlesec package)
\titleformat{\section}{\Large\scshape\raggedright}{}{0em}{}[\titlerule]
\titlespacing{\section}{0pt}{0.5em}{0.5em}

% Column formatting (array, tabularx packages)
\setlength\tabcolsep{5pt}
\newcolumntype{D}{>{\raggedleft \arraybackslash}p{1.55cm}}
\newcolumntype{I}{>{\raggedright \arraybackslash}X}  % \hangindent=1em

% Header (fancyhdr package)
\pagestyle{fancy}
\fancyhf{}
\renewcommand{\headrulewidth}{0pt}
\renewcommand{\footrulewidth}{0pt}
\lhead{Michael Poehlmann}
\cfoot{\thepage~/~{\hypersetup{linkcolor=black}\pageref{LastPage}}}

\fancypagestyle{first}{
  \fancyhf{}
  \renewcommand{\headrulewidth}{0pt}
  \renewcommand{\footrulewidth}{0pt}
  \cfoot{\thepage~/~{\hypersetup{linkcolor=black}\pageref{LastPage}}}
}


% Custom commands and settings
\newcommand{\UCD}{\textbf{University of California, Davis\xspace}}
\newcommand{\UMN}{\textbf{University of Minnesota\xspace}}